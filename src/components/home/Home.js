// import React from 'react'
import Hero from '../hero/Hero.js';

const Home = ({events}) => {
    return(
        <div>
           <Hero events = {events} />
        </div>
    )
}
export default Home